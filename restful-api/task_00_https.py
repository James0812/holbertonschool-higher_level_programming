HTTP (HyperText Transfer Protocol) est le protocole de base du web permettant la communication entre un client (navigateur ou application) et un serveur. Il sert à envoyer des requêtes et recevoir des réponses, mais il n’est pas sécurisé car les données circulent en clair. HTTPS (HyperText Transfer Protocol Secure) est la version sécurisée de HTTP ; il utilise un système de chiffrement (SSL/TLS) pour protéger les données contre l’espionnage et la modification. Une communication HTTP repose sur un modèle simple : le client envoie une requête (contenant une méthode comme GET ou POST, une ressource et des en-têtes), puis le serveur renvoie une réponse (avec un code de statut, des en-têtes et un corps de réponse). Les principales méthodes HTTP sont GET (récupérer des données), POST (envoyer ou créer des données), PUT (modifier des données) et DELETE (supprimer des données). Les codes de statut HTTP permettent de comprendre le résultat d’une requête, par exemple 200 (succès), 201 (création réussie), 301 (redirection), 404 (ressource introuvable) et 500 (erreur serveur). Ensemble, HTTP et HTTPS constituent la base de toutes les communications entre navigateurs, serveurs et API sur le web moderne.
